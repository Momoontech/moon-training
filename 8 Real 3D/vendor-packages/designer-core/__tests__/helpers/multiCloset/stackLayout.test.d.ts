/**
 * Pure unit tests for the stack layout math in `helpers/multiCloset/stackLayout.ts`.
 * Uses step = 10, thickness = 1 so a stack of height `10·N − 1` tiles into `N` holes
 * with exact integer arithmetic.
 */
export {};
