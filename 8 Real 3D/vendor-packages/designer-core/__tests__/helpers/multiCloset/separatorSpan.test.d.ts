/**
 * Unit tests for `separatorSpan` (`components/Node/helpers/effects.ts`) — the pure
 * 1-D span a multiCloset separator covers to bracket its adjacent sections on one
 * axis. Used per axis by `updateMultiClosetItemLayoutEffect` for both depth (z,
 * top view) and height (y, front / wall-elevation view). Kept pure so the span
 * math is verified without a full Item/catalog fixture.
 */
export {};
