/**
 * Tests for `helpers/multiCloset/systemNeeds` — back-end need names → the section desire vector.
 *
 * The names are authored server-side as free text, so the two things worth pinning are the
 * NORMALIZATION (which spellings collapse onto one registry entry) and the "one need = one unit"
 * counting rule. The strings below are verbatim from a real `Custom Closet` system.
 */
export {};
