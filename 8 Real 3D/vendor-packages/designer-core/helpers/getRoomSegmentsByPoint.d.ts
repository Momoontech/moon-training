import { CoreDesigner } from '../designer-core';
import { UUID } from '../declarations';
declare const getRoomSegmentsByPoint: (core: CoreDesigner, pointId: UUID) => UUID[];
export default getRoomSegmentsByPoint;
