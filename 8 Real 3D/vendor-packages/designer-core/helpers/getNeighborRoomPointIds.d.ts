import { CoreDesigner } from '../designer-core';
import { UUID } from '../declarations';
declare const getNeighborRoomPointIds: (core: CoreDesigner, pointId: UUID) => UUID[];
export default getNeighborRoomPointIds;
