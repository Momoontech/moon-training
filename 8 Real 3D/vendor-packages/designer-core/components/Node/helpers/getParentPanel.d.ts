import { CoreDesigner } from '../../../designer-core';
import { UUID } from '../../../declarations';
declare const getParentPanel: (core: CoreDesigner, nodeId: UUID | undefined) => import("..").Panel;
export default getParentPanel;
