import { CoreDesigner } from '../../../designer-core';
import { UUID } from '../../../declarations';
declare const getMountPoint: (core: CoreDesigner, nodeId: UUID | undefined) => import("..").MountPoint;
export default getMountPoint;
