import { UUID } from '../declarations';
import type { CoreDesigner } from '../designer-core';
declare const getRoomBySegment: (core: CoreDesigner, segmentId: UUID) => UUID;
export default getRoomBySegment;
