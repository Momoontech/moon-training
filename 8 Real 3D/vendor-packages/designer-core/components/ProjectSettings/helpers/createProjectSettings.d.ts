import { ProjectSettings } from '..';
import { CoreMode, IProjectSettings } from '../../../declarations';
import { CoreDesigner } from '../../../designer-core';
export declare function createProjectSettings(core: CoreDesigner, projectSettingsDB: IProjectSettings, coreMode: CoreMode): ProjectSettings;
