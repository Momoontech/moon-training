import { UUID } from '../../../declarations';
import { CoreDesigner } from '../../../designer-core';
declare const getBoxContainer: (core: CoreDesigner, boxContainerId?: UUID | undefined) => import("..").BoxContainer;
export default getBoxContainer;
