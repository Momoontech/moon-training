import { Vector2 } from '../Vector2';
/**
 * Checks if two segments are collinear (parallel and on the same line)
 */
export declare function checkSegmentsCollinear(seg1From: Vector2, seg1To: Vector2, seg2From: Vector2, seg2To: Vector2, tolerance?: number): boolean;
