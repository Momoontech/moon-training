import { UUID } from '../../../declarations';
import { CoreDesigner } from '../../../designer-core';
declare const getCountertop: (core: CoreDesigner, countertopId?: UUID | undefined) => import("..").Countertop;
export default getCountertop;
