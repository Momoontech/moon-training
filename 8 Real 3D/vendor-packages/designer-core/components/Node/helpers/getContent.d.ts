import { UUID } from '../../../declarations';
import { CoreDesigner } from '../../../designer-core';
declare const getContentPart: (core: CoreDesigner, partId?: UUID | undefined) => import("..").Part;
export default getContentPart;
