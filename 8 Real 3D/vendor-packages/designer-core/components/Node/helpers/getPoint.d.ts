import { CoreDesigner } from '../../../designer-core';
import { UUID } from '../../../declarations';
declare const getPoint: (core: CoreDesigner, pointId?: UUID | undefined) => import("..").Point;
export default getPoint;
