const getNegativeShapeOffset = () => {
    // const points = shape.extractPoints(4);
    // const box2 = new Box2().setFromPoints(points.shape);
    // return -box2.min.x;
    return 0;
};

export { getNegativeShapeOffset as default };
